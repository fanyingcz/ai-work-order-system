# 复制为 .env 后填入真实值：cp .env.example .env
# 本文件进版本库（内容已脱敏），.env 不进

# --- MySQL ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=AI_Work_Order

# --- LLM（DeepSeek）---
ds_apikey=

# --- 腾讯地图（地址校验，必填）---
# 申请：https://lbs.qq.com/dev/console/key/manage  个人开发者免费，即时开通
# 缺失后果：AddressHandler.process() 直接返回 ERROR、reliability=None，
#   service.py:83 要求 reliability>=7 且 district=='浦东新区' 不满足，
#   报修文本会在地址环节被拒 —— 查询/派单照常，但"文本→生成工单"走不通。
# 演示时注意：报修文本要带「浦东新区」前缀，否则地址可信度不够。
lbs_key=
